each prod in allProds
    div.card.mt-5.mx-auto(style='width: 18rem;')
        img.card-img-top(src=prod.img alt='')
        div.card-body
            h5.card-title=prod.title
            p.card-text=`STOCK: ${prod.stock}`
            p.card-text=`PRECIO: $ ${prod.price}`